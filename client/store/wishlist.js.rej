diff a/client/store/wishlist.js b/client/store/wishlist.js	(rejected hunks)
@@ -14,2 +14,2 @@ export const getWishlist = user => {
-    const { data } = await axios.get(`./api/users/${user}/wishlist`)
-    let map = { size: 0 }
+    const {data} = await axios.get(`./api/users/${user}/wishlist`)
+    let map = {size: 0}
@@ -40 +40 @@ export const removeWishlist = user => {
-export default (state = { wishlistMap: { size: 0 } }, action) => {
+export default (state = {wishlistMap: {size: 0}}, action) => {
@@ -43 +43 @@ export default (state = { wishlistMap: { size: 0 } }, action) => {
-      return { ...state, wishlistMap: action.wishlist }
+      return {...state, wishlistMap: action.wishlist}
